; definicion de la función de fibonacci
; La serie de fibonacci está dado por :
; F(0)=0, F(1)=1,F(2)=1, F(n)= F(n-1)+ F(n-2), si n>1

(define (fibo n)
  ;condición de paro, cuando n<2 retorna n, sino fibo(n -1) + fibo (n-2)
  (if (< n 2)
       n
      (+ (fibo (- n 1)) (fibo (- n 2))) ; es igual a fibo(n -1) + fibo (n-2)
         
       )

  )
